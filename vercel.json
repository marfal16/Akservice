{
  "buildCommand": "cd client && npm run build && cp -a dist/. ../",
  "outputDirectory": ".",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
